#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main(){
    freopen("../input.txt", "r", stdin);
    freopen("../output.txt", "w", stdout);

    int n, t; cin >> n >> t;
    map<int, pair<int, int>> m; // time moment :
    for (int i = 1; i <= t; ++i){
        int m; cin >> m;
        for (int i = 0; i < m; ++i){
            m[i].
        }
    }
}
